Build a high-end, client-closing demo website hero section for a premium optical / eyewear company using the attached image frames folder as the main visual animation.

The goal is to make the website feel like a luxury international eyewear brand — elegant, minimal, modern, smooth, expensive, and visually impressive enough to present directly to a client.

TECH STACK:
Use React + Vite + Tailwind CSS.
Use GSAP + ScrollTrigger for premium motion and scroll interactions.
Make the website fully responsive, with mobile-first priority because the provided frames are designed for vertical mobile composition.

MAIN HERO ANIMATION:
Use the attached frames in their correct sequence to create a smooth frame-by-frame hero animation.

Do not replace the images with placeholders.
Automatically inspect the frames folder, detect the available images, sort them correctly by filename/number, and use those exact files.

The image sequence should feel similar to an Apple-style product animation.

On mobile:

* Use a sticky/full-screen 100svh hero.
* Keep the animation centered and visually dominant.
* Use object-fit: cover carefully without cutting off the eyeglasses.
* Maintain the important product inside a central mobile-safe area.
* Scroll should smoothly progress through the image sequence.
* The animation should feel fluid, premium
